# - name: conditions using ansible_facts
#   hosts: frontend
#   tasks:
#   - name: print if redhat
#     debug:
#       msg: "OS family is Redhat, package manager is dnf" # H should me caps
#     when: ansible_facts.os_family == "Redhat"  # same issue

#   - name: print if Debian
#     debug:
#       msg: "OS family is Redhat, package manager is apt-get"
#     when: ansible_facts.os_family == "Debian"



- name: conditions using ansible_facts
  hosts: frontend
  tasks:
    - name: print if redhat
      debug:
        msg: "OS family is RedHat, package manager is dnf"
      when: ansible_facts.os_family == "RedHat"

    - name: print if Debian
      debug:
        msg: "OS family is Debian, package manager is apt-get"
      when: ansible_facts.os_family == "Debian"
